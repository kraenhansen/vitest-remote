{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    "moduleResolution": "Bundler",
    "customConditions": ["require", "import", "react-native"],
    "types": ["react-native"]
  }
}
